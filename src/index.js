// what the react-scripts will look for to render the app

import React from 'react';
import ReactDOM from 'react';

import App from './App';
import "./index.css";

ReactDOM.render(<App />, document.getElementById('root'));